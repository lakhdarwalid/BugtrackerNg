export enum apiUrls {
    roles = "/roles",
    users = "/users",
    app = "/apps",
    activities = "/activities",
    auth = "/authenticate", 
    bugs = "/bugs",
    passwordrecovery = "/passwordrecovery"

}